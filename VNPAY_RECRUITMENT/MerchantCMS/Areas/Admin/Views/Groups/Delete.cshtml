@model VNPRECRUITMENT.Models.Group

@{
    ViewBag.Title = "Xóa phòng ban";
}


<div>
    <div class="page-header">
        <h1>@ViewBag.title</h1>
    </div>
    <dl class="dl-horizontal">
        <dt>
            Phòng Ban
        </dt>

        <dd>
            <label  name="groupname">@ViewBag.SelectById</label>
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @*@Html.AntiForgeryToken()*@

    <div class="clearfix form-actions center">
        <button type="submit" class="btn btn-primary btn-sm" onclick="alert('Đã xóa')">Xóa</button>
        <a href="@Url.Action("Index", "Groups")" class="btn btn-dark-grey btn-sm">Hủy bỏ</a>
    </div>
    }
</div>
