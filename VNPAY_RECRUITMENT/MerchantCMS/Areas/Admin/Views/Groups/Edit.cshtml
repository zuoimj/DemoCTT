@model VNPRECRUITMENT.Models.Group

@{
    ViewBag.Title = "Chỉnh sửa";
}

@using (Html.BeginForm())
{
    @*@Html.AntiForgeryToken()*@
    
    <div class="form-horizontal">
        <div class="page-header">
            <h1>@ViewBag.title</h1>
        </div>
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.GROUP_ID)

        <div class="form-group">
            <label class="col-sm-2 control-label">Phòng ban</label>
            <div class="col-md-10">
                @*@ViewBag.SelectById
                @Html.EditorFor(model => model.GROUP_NAME, new { htmlAttributes = new { @class = "form-control" } })*@
                <div class="co  l-sm-10">
                    <input type="text" name="groupname" value="@ViewBag.SelectById" placeholder="Nhập từ khóa" class="form-control">
                </div>
                @*@Html.ValidationMessageFor(model => model.GROUP_NAME, "", new { @class = "text-danger" })*@
            </div>
        </div>

        <div class=" clear"></div>

        <div class="clearfix form-actions center">
            <button type="submit" class="btn btn-primary btn-sm">Save</button>
            <a href="@Url.Action("Index", "Groups")" class="btn btn-dark-grey btn-sm">Hủy bỏ</a>
        </div>
    </div>
}

<div>
</div>
